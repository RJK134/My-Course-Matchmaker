{
  "name": "mycoursematchmaker",
  "private": true,
  "version": "0.0.0",
  "description": "MyCourseMatchmaker — course-matching platform. This root package.json hosts repo-level scripts (datalake sync, search indexer); the React frontend lives in `frontend/` and the Express API in `api/`.",
  "type": "module",
  "scripts": {
    "sync-datalake": "node scripts/sync-datalake.mjs",
    "index-courses": "node scripts/index-courses.mjs",
    "test": "node --test scripts/datalake/*.test.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "meilisearch": "^0.45.0",
    "pg": "^8.13.3"
  }
}
